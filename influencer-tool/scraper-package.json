{
  "name": "influencer-scraper",
  "version": "1.0.0",
  "description": "Local Playwright scraper companion — run alongside vercel dev",
  "private": true,
  "scripts": {
    "start": "node scraper.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
